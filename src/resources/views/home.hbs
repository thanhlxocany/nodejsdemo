<section class="hero">
  <h1>{{heading}}</h1>
  <p class="description">{{description}}</p>

  <ul class="features">
    {{#each features}}
      <li class="feature">{{this}}</li>
    {{/each}}
  </ul>
</section>
